NEXT_PUBLIC_CONTRACT_ADDRESS=0xYourDeployedContractAddress
NEXT_PUBLIC_CHAIN_ID=11155111
# Optional: force the public origin used to build QR codes and /verify links
# (e.g. your production URL). Leave unset to fall back to window.location.origin.
NEXT_PUBLIC_APP_ORIGIN=
